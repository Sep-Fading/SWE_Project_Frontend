export type User = {
    firstName: string;
    lastName: string;
    email: string;
    address: string;
    phoneNumber: string;
    taxCode: string;
    accountNumber: string;
    sortCode: string;
  }